# Repository Instructions

At the start of every task in this repository, read `AGENT_LEARNINGS.md`.

Before switching branches, creating branches, building, or doing write-heavy
work, follow the worktree strategy in `AGENT_LEARNINGS.md`: keep active branch
work in a separate sibling worktree and keep `/home/rorym/cuml` as the primary
checkout.

For language-specific work, also read:

- `python/agents.md` for Python changes and reviews.
- `cpp/agents.md` for C++/CUDA changes and reviews.
